module FormatSwitch (
	input  Clk1,
	input  Clk2,
	input  RequestFormat,
	input  Sync,
	output ClockOut,
	output reg FormatOut
)

	assign ClockOut = (FormatOut && Clk1) || (~FormatOut && Clk2);

	always @(posedge Fsn) begin
		FrameFormat <= RequestFormat
	end
	
endmodule